# Fast end-to-end check of the whole pipeline (minutes, not hours).
# Not an experiment: the episode budget is far too small to learn anything.
algo:
  n_episodes: 3
  eval_interval: 3
  n_eval_episodes: 1
run:
  visdom_enabled: false
