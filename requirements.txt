# Core dependencies. Tested with Python 3.11.
torch>=2.0          # policy / value networks and PPO updates
numpy>=1.24
pandas>=2.0         # result aggregation
scipy>=1.10         # paired t-test and Wilcoxon signed-rank test
matplotlib>=3.7     # draft figures
PyYAML>=6.0         # configuration files
visdom>=0.2         # optional: live training curves; training runs without it
